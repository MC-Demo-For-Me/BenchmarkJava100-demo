/* This file was generated by AppSecAI to validate the fix for CWE-79 in src/main/java/org/owasp/benchmark/testcode/BenchmarkTest00492.java:55.
Inputs below are representative attacks for this CWE and should be rejected by the fix. Delete or edit this file after review. */
package appsecai;

import static org.junit.jupiter.api.Assertions.assertFalse;
import java.util.Arrays;
import java.util.List;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

public class cwe79GeneratedTest {
    @Test
    @Disabled("Wire this generated test to the fixed entry point before relying on it in CI.")
    void generatedNegativeCase() {
        List<String> maliciousInputs = Arrays.asList("<script>alert(1)</script>", "\"><img src=x onerror=alert(1)>");
        List<String> safeInputs = Arrays.asList("plain-text", "Hello, world!");
        assertFalse(maliciousInputs.isEmpty());
        assertFalse(safeInputs.isEmpty());
    }
}
