/* This file was generated by AppSecAI to validate the fix for CWE-78 in src/main/java/org/owasp/benchmark/testcode/BenchmarkTest00293.java:76.
Inputs below are representative attacks for this CWE and should be rejected by the fix. Delete or edit this file after review. */
package appsecai;

import static org.junit.jupiter.api.Assertions.assertFalse;
import java.util.Arrays;
import java.util.List;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

public class cwe78GeneratedTest {
    @Test
    @Disabled("Wire this generated test to the fixed entry point before relying on it in CI.")
    void generatedNegativeCase() {
        List<String> maliciousInputs = Arrays.asList("test; cat /etc/passwd", "$(whoami)");
        List<String> safeInputs = Arrays.asList("customer-sync", "daily-report");
        assertFalse(maliciousInputs.isEmpty());
        assertFalse(safeInputs.isEmpty());
    }
}
