/* This file was generated by AppSecAI to validate the fix for CWE-89 in src/main/java/org/owasp/benchmark/testcode/BenchmarkTest00328.java:65.
Inputs below are representative attacks for this CWE and should be rejected by the fix. Delete or edit this file after review. */
package appsecai;

import static org.junit.jupiter.api.Assertions.assertFalse;
import java.util.Arrays;
import java.util.List;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

public class cwe89GeneratedTest {
    @Test
    @Disabled("Wire this generated test to the fixed entry point before relying on it in CI.")
    void generatedNegativeCase() {
        List<String> maliciousInputs = Arrays.asList("' OR '1'='1", "'; DROP TABLE users; --");
        List<String> safeInputs = Arrays.asList("customer-123", "invoice-42");
        assertFalse(maliciousInputs.isEmpty());
        assertFalse(safeInputs.isEmpty());
    }
}
